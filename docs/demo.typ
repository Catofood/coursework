// Демонстрация использования шаблона ГОСТ для курсовых работ

// 1. ИМПОРТ ШАБЛОНА
#import "lib/gost.typ": *

// 2. ИНИЦИАЛИЗАЦИЯ (обязательно!)
#show: init

// 3. ГЛАВЫ БЕЗ НУМЕРАЦИИ (для Введения, Заключения, Списка источников)
#ch("ВВЕДЕНИЕ")

Текст введения с автоматическим выравниванием по ширине, красной строкой (1.25см) и межстрочным интервалом 1.5. Шрифт Times New Roman 14pt применяется автоматически.

Цель работы: разработать программу для решения задачи.

Задачи:
- Анализ предметной области
- Проектирование структуры
- Реализация и тестирование

#pagebreak()

// 4. ГЛАВЫ С НУМЕРАЦИЕЙ
= Анализ предметной области

Первый уровень заголовка (нумерация "1"). Автоматически выравнивается по левому краю, полужирный, 14pt.

== Постановка задачи

Второй уровень заголовка (нумерация "1.1").

=== Подзадача

Третий уровень (нумерация "1.1.1") также поддерживается.

#v(0.8em)

== Списки

Маркированные списки:
- Элемент списка с длинным текстом, который автоматически переносится на следующую строку с правильным отступом
- Второй элемент
- Третий элемент

Нумерованные списки:
1. Первый пункт
2. Второй пункт с описанием, которое может занимать несколько строк
3. Третий пункт

#v(0.8em)

== Таблицы

Простая таблица с подписью сверху:

#figure(
  table(
    columns: (0.5fr, 2fr, 1.5fr),
    align: horizon,
    [*№*], [*Наименование*], [*Значение*],
    [1], [Параметр A], [100],
    [2], [Параметр B], [200],
    [3], [Параметр C], [300],
  ),
  caption: [Пример простой таблицы],
  kind: table
) <table-simple>

Ссылка на таблицу в тексте: см. таблицу @table-simple.

#v(0.8em)

Многостраничная таблица с автоматическим заголовком "Продолжение таблицы":

#figure(
  long-table(
    columns: (0.5fr, 2fr, 1.5fr),
    [*№*], [*Описание*], [*Примечание*],
    [1], [Элемент один], [Примечание 1],
    [2], [Элемент два], [Примечание 2],
    [3], [Элемент три], [Примечание 3],
    
  ),
  caption: [Многостраничная таблица],
  kind: table
) <table-long>

#v(0.8em)

== Рисунки и изображения

// Для изображений создайте папку images/
// #figure(
//   image("images/screenshot_1.png", width: 70%),
//   caption: "Пример диаграммы"
// ) <fig-diagram>

// Ссылка на рисунок: см. рисунок @fig-diagram

#v(0.8em)

== Математические формулы

Inline формула: скорость $v = s / t$, где $s$ -- расстояние, $t$ -- время.

Формула блоком с автоматической нумерацией:

$
S = pi r^2
$ <eq-area>

Площадь круга вычисляется по формуле @eq-area.

Сложная формула:

$$
x\_(1,2) = (-b plus.minus sqrt(b^2 - 4a c)) / (2a)
$$

#v(0.8em)

== Исходный код

Inline код: используйте метод `main()` для запуска.

Блок кода:

```java
public class Example {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```

// Код из файла (если файл существует):
// #figure(
//   raw(read("../code/main.cpp"), lang: "cpp", block: true),
//   caption: [Пример кода из файла]
// )

#pagebreak()

= Разработка программы

== Проектирование

Здесь описание архитектуры, структур данных и алгоритмов.

== Реализация

Описание реализованных функций с примерами кода.

#pagebreak()

= Тестирование

Результаты тестирования с таблицей тест-кейсов:

#figure(
  table(
    columns: (0.4fr, 1.5fr, 1.5fr, 1.5fr),
    align: (center, left, left, left),
    table.header(
      [*№*], [*Тест-кейс*], [*Ожидаемый результат*], [*Фактический результат*]
    ),
    [1], [Запуск программы], [Открывается главное окно], [✓ Пройден],
    [2], [Добавление элемента], [Элемент добавлен в список], [✓ Пройден],
    [3], [Удаление элемента], [Элемент удален из списка], [✓ Пройден],
  ),
  caption: [Результаты тестирования],
  kind: table
)

// Скриншоты тестов:
// #figure(
//   image("images/test-1.png", width: 80%),
//   caption: "Скриншот теста 1"
// ) <test-1>

#pagebreak()

#ch[ЗАКЛЮЧЕНИЕ]

#v(0.8em)

В ходе выполнения курсовой работы была достигнута поставленная цель. Разработанная программа полностью соответствует требованиям задания.

Результаты работы:
1. Проведен анализ предметной области
2. Спроектирована архитектура программы
3. Реализован функционал
4. Проведено тестирование

#pagebreak()

#ch("СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ")

#v(0.8em)

+ Автор А.А. Название книги. Издательство, 2020. 500 с.

+ Автор Б.Б., Автор В.В. Название статьи // Название журнала. 2021. №3. С. 15-25.

+ Официальная документация [Электронный ресурс]. URL: https://example.com (дата обращения: 17.12.2025).

#pagebreak()

#{
  show heading: none
  align(heading([ПРИЛОЖЕНИЕ A. Код программы], numbering: none), center)
}

#align([*ПРИЛОЖЕНИЕ A. \ Код программы*], center)

#v(0.8em)

Main.java
```java
public class Main {
    public static void main(String[] args) {
        System.out.println("Пример программы");
    }
}
```

// Или из файла:
// #raw(read("../code/Main.java"), lang: "java", block: true)
